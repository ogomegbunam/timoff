import 'package:flutter/material.dart';
class StaffsOnLeave extends StatelessWidget {
  const StaffsOnLeave({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('staffs onleave'),),
      ),
    );
  }
}
