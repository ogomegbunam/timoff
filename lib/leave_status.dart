import 'package:flutter/material.dart';
class LeaveStatus extends StatelessWidget {
  const LeaveStatus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text(' leave status'),),
      ),
    );
  }
}
